version: "2"
services:
  lava-slave0:
    hostname: worker0
    restart: always
    build:
      context: ./
    volumes:
      - /dev:/dev
      - /boot:/boot
      - /lib/modules:/lib/modules
      - /sys/class/gpio:/sys/class/gpio:rw
    privileged: true
    ports:
      - "69:69" # tftp

networks:
  default:
    external:
      name: lavanet
